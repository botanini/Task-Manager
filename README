# To-do List Final Project

## Install and Run Instructions

### Step 1: Ensure Python is Installed

Download Python from the official website.
Verify your installation by running:
`py --version`

Step 2: Install Required Packages
To install all necessary packages, run:
`pip install -r requirements.txt`

Step 3: Run the Code
Run the Python script using:
`py app.py`
follow the prompts

## Included Files
    static(folder):
        style.css: the styling for the web to look more than the typical standard app
    template(folder):
        index.html: this is the html that you can see when running this app
    app.py: the code for the app
    requirements.txt: Lists the necessary packages to run the code.
